{
  "name": "sosumi-mcp",
  "version": "1.1.0",
  "description": "A local stdio MCP server that renders Apple documentation as Markdown",
  "license": "MIT",
  "author": "NSHipster and Austin Traver",
  "repository": {
    "type": "git",
    "url": "https://github.com/austintraver/sosumi-mcp.git"
  },
  "packageManager": "pnpm@11.25.0",
  "type": "module",
  "engines": {
    "node": ">=26.0.0"
  },
  "scripts": {
    "build": "pnpm run typecheck && node scripts/build-stdio-bundle.mjs",
    "dev": "tsx src/stdio.ts",
    "pack:mcpb": "pnpm run build && pnpm run stage:mcpb && mcpb validate artifacts/stage && mcpb pack artifacts/stage artifacts/sosumi-mcp-1.1.0.mcpb",
    "start": "pnpm run build && node dist/stdio.js",
    "stage:mcpb": "node scripts/stage-mcpb.mjs",
    "verify:mcpb": "node scripts/verify-mcpb.mjs artifacts/sosumi-mcp-1.1.0.mcpb",
    "typecheck": "tsc --noEmit --project tsconfig.build.json",
    "test": "vitest run",
    "test:search:live": "SOSUMI_LIVE_SEARCH=1 vitest run tests/search.live.test.ts",
    "test:watch": "vitest",
    "format": "biome check --write --linter-enabled=false .",
    "lint": "biome check --formatter-enabled=false .",
    "check": "biome check --write .",
    "check:ci": "biome check ."
  },
  "dependencies": {
    "@modelcontextprotocol/server": "2.0.0",
    "zod": "4.5.4"
  },
  "devDependencies": {
    "@anthropic-ai/mcpb": "2.1.2",
    "@biomejs/biome": "2.5.11",
    "@types/node": "26.4.0",
    "esbuild": "0.28.2",
    "tsx": "4.23.13",
    "typescript": "7.0.2",
    "vitest": "4.1.11"
  }
}
